{
  "name": "@nexly-challenge/root",
  "private": true,
  "workspaces": [
    "apps/*",
    "mock-integrations",
    "scripts"
  ],
  "scripts": {
    "start:dev": "npm run start:dev -w @nexly-challenge/api",
    "api:dev": "npm run dev -w @nexly-challenge/api",
    "generate-events": "npm run generate-events -w @nexly-challenge/scripts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@fastify/static": "^7.0.4"
  }
}
